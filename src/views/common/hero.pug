mixin hero (mod, advancedClasses)
  section(class=`hero hero--${mod} ${advancedClasses || ''}`)
    -
      var socials = [
        ['vk', 'http://vk.com'],
        ['gh', 'http://gh.com'],
        ['in', 'http://in.com']
      ]

    .hero__links
      ul.socials
        each item in socials
          li.socials__item
            a(href=`${item[0]}`).socials__link
              +icon(item[1], 'socials__link-icon')
      .hero__menu
        a(href="").hamburger

      .hero__title
        .section-title
          img(src="", alt="").section-title__pic

        .user.hero__user
          .user__pic
            img.user__pic-img
          .user__description
            .user__name Владимир Астахов
            .user__info Личный сайт разработчика

      a(href="").down-btn.hero__down-btns
